{% set name = "DeepSpectrum" %}
{% set version = "0.5.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/DeepSpectrum/{{name}}/archive/v{{ version }}.tar.gz
  sha256: 603fbb2d0614dfdbba8a2243569ad54f2e1123bf38d3f851a1946e708cbea0ee

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt
  entry_points:
    - deepspectrum = deepspectrum.__main__:cli


requirements:
  build:
    - python >=3.6,{{PY_VER}}*
    - setuptools

  run:
    - python {{PY_VER}}*
    - mesa-libgl-cos6-x86_64 # [linux]
    - click>=7.0
    - imread>=0.7.1
    - librosa>=0.6.3
    - matplotlib>=3.0.3
    - numpy>=1.16.3
    - opencv>=4.1.0
    - pillow>=6.0.0
    - scipy>=1.2.1
    - tensorflow-gpu>=1.13.0 # [not osx]
    - tensorflow>=1.13.0 # [osx]
    - tqdm>=4.32.1

test:
  source_files:
    - tests/*
    - examples/*
  imports:
    - deepspectrum
  requires:
    - pytest>=4.4.1
    - pytest-cov>=2.7.1
  commands:
    - pytest -vv --cov deepspectrum tests/

about:
  home: https://github.com/DeepSpectrum/DeepSpectrum
  license: GPL v3
  license_file: LICENSE
  license_family: GPL
  dev_url: https://github.com/DeepSpectrum/DeepSpectrum
  summary: 'A tool for extracting deep CNN-descriptors from audio data.'

extra:
  recipe-maintainers:
    - DeepSpectrum
    - mauricege